var wordSplit = function(word){
  if(word.length >4){
    word = word.substring(0,4)+'...';
  }
  return word
};
 module.exports.wordSplit = wordSplit;

 //es6 写法
//  const _wordSplit = wordSplit;
// export { _wordSplit as wordSplit };